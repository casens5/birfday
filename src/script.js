"use strict";
const WEEK_SECONDS = 604800;
const DAY_SECONDS = 86400;
const HOUR_SECONDS = 3600;
const MINUTE_SECONDS = 60;
const SUN_DAY_SECONDS = 2191832;
function $(id) {
    return document.getElementById(id);
}
$("getDatesButton").addEventListener("click", () => {
    console.log("waba");
});
